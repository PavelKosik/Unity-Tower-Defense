using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//makes sure that stats only of one wall are open at the time
public class WallStatsManagerScript : MonoBehaviour
{
    public GameObject currentActiveWallStatsPanel;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
